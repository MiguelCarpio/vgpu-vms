[defaults]
# Give a slow guest more time to answer before a task is called unreachable.
timeout = 30

[ssh_connection]
# Retry the SSH connection on transient failures (e.g. "banner exchange" timeouts
# while a freshly booted guest's sshd is still coming up).
retries = 5
